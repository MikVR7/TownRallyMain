using UnityEngine;
using UnityEngine.UI;

namespace TownRally
{
    internal class PanelRally : MonoBehaviour
    {
        //[SerializeField] private ScrollRect scrollviewIntroTeacher = null;
        //[SerializeField] private RectTransform rtContent = null;
        [SerializeField] private GameObject goIntroTeacher = null;

        internal void Init()
        {

        }

        //private void Update()
        //{
        //    if(Input.GetKeyDown(KeyCode.L))
        //    {
        //        Debug.Log("RT CONTENT: " + rtContent.sizeDelta + " " + rtContent.rect);
        //    }
        //}

        private void OnEnable()
        {
            //if()
            //scrollviewIntroTeacher.content
        }
    }
}
